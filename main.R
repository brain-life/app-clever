#!/usr/bin/env Rscript

main <- function() {
	args <- commandArgs(trailingOnly = TRUE)
	bold_filename <- args[1]
	print(bold_filename)

	## load bold.nii.gz, and convert it to 2d matrix and pass run clever!

	## convert output from clever to CSV and other data product
}
